{
  // Definição do nome do Container 
  "name": "Padrões de Desenvolvimento Web",

  // Indica como a imagem será criada
  "build": {
    "dockerfile": "Dockerfile"
  },

  // Libera as portas do container
  "forwardPorts": [8080, 3306],

  // Efetua personalizações no VScode
  "customizations": {
    "vscode": {
      // Define as extensões que serão instaladas
      "extensions": [
        "ms-azuretools.vscode-docker", // Docker       
        "vscjava.vscode-java-pack",    // Java       
        "vmware.vscode-spring-boot",   // Spring Boot       
        "vscjava.vscode-spring-boot-dashboard", 
        "cweijan.vscode-mysql-client2",
        "vscjava.vscode-spring-initializr"  // MySQL      
      ],

      // Configurações visuais do VScode
      "settings": {
        "workbench.colorTheme": "Dark Modern", // Tema
        "editor.fontSize": 14, // Fonte do editor
        "terminal.integrated.fontSize": 14, // Fonte do terminal
        "java.server.launchMode": "Standard" // Força o Java a iniciar no modo "normal"
      }
    }
  },

  // Executa na criação do container 
  // Configura o MySQL
  "postCreateCommand": "/bin/bash .devcontainer/init-mysql.sh",

  // Executa toda vez que o container é executado
  // inicia o MySQL
  "postStartCommand": "service mysql start"
}
